apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopizer-admin
  namespace: shopizer
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shopizer-admin
  template:
    metadata:
      labels:
        app: shopizer-admin
    spec:
      containers:
        - name: shopizer-admin
          image: ghcr.io/rishabhgupta9695/shopizer-admin:latest
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: shopizer-admin
  namespace: shopizer
spec:
  type: NodePort
  selector:
    app: shopizer-admin
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30200
